{
  "name": "@sovermind/contract",
  "version": "0.1.0",
  "description": "SoverMind Solana smart contract — Anchor framework",
  "scripts": {
    "build":  "anchor build",
    "deploy": "anchor deploy --provider.cluster devnet",
    "test":   "anchor test --provider.cluster devnet",
    "deploy:script": "ts-node scripts/deploy.ts"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/web3.js":   "^1.95.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-mocha":    "^10.0.0",
    "ts-node":     "^10.9.2",
    "typescript":  "^5.5.2"
  }
}
